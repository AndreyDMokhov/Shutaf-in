<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath*:smtp.config.properties" ignore-unresolvable="true"/>


    <bean id="javaMailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl" >
        <property name="protocol" value="${mail.transport.protocol}"/>
        <property name="host" value="${mail.smtp.host}"/>
        <property name="port" value="${mail.smtp.port}"/>
        <property name="username" value="shutafin@gmail.com"/>
        <property name="password" value="shutafin2017k"/>
        <property name="javaMailProperties" >
            <value>
                mail.smtp.auth=${mail.smtp.auth}
                mail.smtp.ssl=${mail.smtp.ssl}
                mail.smtp.socketFactory.port=${mail.smtp.socketFactory.port}
                mail.smtp.socketFactory.class=${mail.smtp.socketFactory.class}
                mail.smtp.starttls.enable=${mail.smtp.starttls.enable}
                mail.smtp.starttls.required=${mail.smtp.starttls.required}
            </value>
        </property>

    </bean>


</beans>